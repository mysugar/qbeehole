apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "beehole-backend.fullname" . }}
  labels:
    {{- include "beehole-backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "beehole-backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "beehole-backend.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: {{ include "beehole-backend.fullname" . }}
  labels:
    {{- include "beehole-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beehole-backend.selectorLabels" . | nindent 4 }}
